<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mt4">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.Settings" />
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".slider.DetailsActivity" />
        <activity android:name=".slider.Slider" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".menu.HomeActivity" />
        <activity android:name=".menu.FailedActivity" />
        <activity android:name=".menu.AbsenActivity" />
        <activity android:name=".activity.LoginDosenActivity" />
        <activity android:name=".menu.DashboadrDosenActivity" />
        <activity android:name=".menu.DashboardActivity" />
        <activity android:name=".menu.ScanActivity" />
        <activity android:name=".activity.LoginActivity" />


        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />
        <activity android:name=".menu.GeneratorActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>